# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Cloudflare wrangler working files (ops/uptime)
.wrangler/

# The dictionary's seed data is content, not code, and stays out of the
# repository: only the notes in docs/seed (FORMAT, SOURCES, README) are tracked.
docs/seed/**/*.json
docs/seed/**/*.txt
